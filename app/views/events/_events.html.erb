<% @events.each do |event| %>
  		<p><%= link_to "#{event.eventname}, #{event.eventlocation}", event %>
  			<span class="small">
  				<%= "#{event.deadline}" %>
  				<!-- | <%= link_to 'Edit', edit_event_path(event) %>-->
  			</span>
  		</p>
  		<hr style="margin-top:-20px">
 <% end %>