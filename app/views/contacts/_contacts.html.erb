    <% @contacts.each do |contact| %>
         <p><%= link_to "#{contact.name}, #{contact.company}", contact %><span class="small"><%= link_to 'Edit', edit_contact_path(contact) %></span></p><hr style="margin-top:-20px">
    <% end %>